# Defines a simple query to ensure the schema is valid.
# This will be expanded with your application's data models.
type Query {
  # A placeholder query to verify the service is running.
  hello: String
}
