connectorId: postgres-connector
connectorType: POSTGRES
# The database name will be provisioned by Firebase.
# The connection string will be provided by environment variable at runtime.
database: ${db.name}
