# Este arquivo foi gerado pelo Firebase.
# Para saber mais, acesse https://firebase.google.com/docs/data-connect
connectorId: postgres-connector
connectorType: POSTGRES
# O nome do banco de dados será provisionado pelo Firebase.
# A string de conexão será fornecida por variável de ambiente em tempo de execução.
database: ${db.name}
